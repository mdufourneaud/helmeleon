# helmeleon

Helmeleon is an Helm chart that renders manifests provided as values.

## Purpose

Helmeleon Helm chart allows to benefit from a subset of Helm templating without needing to manage an Helm chart, by rendering templates provided as values.
